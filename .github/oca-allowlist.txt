# OCA offline fallback fixture (PoC only).
#
# Used by .github/workflows/oca-check.yml ONLY when the live Oracle contributor
# list (https://oca.opensource.oracle.com/?ojr=contrib-list) is unreachable, or
# for the Oracle-employee case where the author is verified by `oracle` GitHub
# org membership rather than by signing the OCA (employees don't appear on the
# public list). Production replaces this file with the org-membership API check.
#
# One identifier per line: a GitHub login or a commit email.
RidhaOracle
ridha@example.com
